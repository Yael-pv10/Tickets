package com.auditorio.tickets.modules.venue.model;

public enum SectionType {
    /** Sección con asientos numerados que se eligen en el mapa. */
    SEATED,
    /** Admisión general: se vende cupo, sin elegir butaca. */
    GENERAL_ADMISSION
}
