spring:
  jpa:
    show-sql: false

server:
  error:
    include-message: never
    include-stacktrace: never
    include-binding-errors: never

logging:
  level:
    root: WARN
    com.auditorio.tickets: INFO
